# 설치방법: pip install -r requirements-train.txt

# 기존 패키지 정리 (선택사항)
pip uninstall numpy opencv-python librosa numba scipy -y

# AI Model Training Requirements (Video + Audio)
# Core ML/DL
torch>=2.0.0,<2.3.0
torchvision>=0.15.0

# Data Processing
numpy>=2.0.0,<2.1.0
pandas>=2.0.0

# 학습 및 실험용 (Video + Audio 공통)
tensorboard>=2.13.0
matplotlib>=3.7.0
seaborn>=0.12.0
scikit-learn>=1.3.0

# Configuration
pyyaml>=6.0

# Image Augmentation (Video용)
albumentations>=1.3.0

# Jupyter
jupyter>=1.0.0
ipykernel>=6.0.0

# Audio 전처리용
librosa>=0.10.0
soundfile>=0.12.1
scipy>=1.10.0

# API (Video + Audio 추론용)
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6
pydantic>=2.0.0

# Utilities (Video + Audio 공통)
tqdm>=4.65.0
pillow>=9.0.0